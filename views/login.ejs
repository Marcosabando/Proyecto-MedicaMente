<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/login.css">
</head>
<body>
  <header><%- include("modules/navbar.ejs") %></header>
    <div class="container d-flex justify-content-center align-items-center vh-100">
        <div class="login-card p-4 w-100" style="max-width: 400px;">
            <h3 class="text-center">Iniciar Sesión</h3>
            <form action="/hospital/login" method="post">
                <div class="mb-3">
                    <label for="inputEmail" class="form-label">Email:</label>
                    <input type="email" class="form-control" id="inputEmail" name="email" required>
                </div>
                <div class="mb-3">
                    <label for="inputPassword" class="form-label">Contraseña:</label>
                    <input type="password" class="form-control" id="inputPassword" name="password" required>
                </div>
                <p class="text-danger"><%= typeof message != 'undefined' ? message : '' %></p>
                <button type="submit" class="btn btn-success w-100">Ingresar</button>
                <button type="button" class="btn btn-secondary w-100 mt-2" onclick="window.location.href='/'">Cancelar</button>
            </form>
            <p class="mt-3 text-center">¿No tienes cuenta? <a href="/hospital/register">Regístrate aquí</a></p>
            <div class="text-center mt-3">
                <img src="/images/MedicaMente.png" alt="" class="img-fluid">
            </div>
        </div>
    </div>
    <footer><%- include("modules/footer.ejs") %></footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>